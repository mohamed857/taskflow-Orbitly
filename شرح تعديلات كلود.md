# شرح تعديلات كلود (Claude)

هذا المستند يشرح **كل** التعديلات التي أُجريت على مشروع TaskFlow (الخادم `taskflow` والواجهة `taskflow-Orbitly`): ماذا تغيّر، **لماذا**، و**كيف تراجعه/تتحقّق منه**. العمل تمّ على فرع `عبدالرحمن` في المستودعين.

## نظرة عامة

- **الخادم (`taskflow`)**: Spring Boot 4.1 + Java 21 + PostgreSQL + Redis + JWT.
- **الواجهة (`taskflow-Orbitly`)**: React 18 + Vite + Tailwind.
- **ما تم إنجازه**: مراجعة شاملة للكود، ثم إصلاح كل الأخطاء المكتشفة، ثم إضافة ميزات جديدة — عبر 4 مراحل.
- **طريقة التطبيق**: كل مرحلة طُبِّقت كـ patch منفصل، ثم جُمِّعت في commit واحد على فرع `عبدالرحمن`.

### كيف تشغّل المشروع للتحقق العملي

```bash
# الخادم (يبني ويشغّل داخل Docker، مع Postgres وRedis محليَّين — لا يحتاج أسراراً)
cd taskflow && docker compose up --build      # يعمل على http://localhost:8080

# الواجهة
cd taskflow-Orbitly && npm install && npm run dev   # يعمل على http://localhost:5173
```

توثيق الـ API متاح على `http://localhost:8080/swagger-ui.html`.

### كيف تراجع الفروق (diff) بالكامل

```bash
git checkout عبدالرحمن
git log --oneline -1          # الـ commit الذي يحوي كل التغييرات
git show --stat HEAD          # قائمة الملفات المتغيّرة
git show HEAD                 # الفروق الكاملة
```

أو عبر صفحة الـ Pull Request على GitHub.

---

## المرحلة 1 — إصلاح الأخطاء + حقل الاسم + تسجيل الدخول المزدوج

### أخطاء الخادم

**1) المسح الدوري يتجاهل المهام «قيد التنفيذ»**
- الملف: `repository/TaskRepository.java` (`markOverDueTask`).
- التغيير: شرط الاستعلام أصبح `status IN ('PENDING','IN_PROGRESS')` بدل `PENDING` فقط.
- السبب: أي مهمة `IN_PROGRESS` يتجاوز موعدها لم تكن تُعلَّم `OVERDUE` أبداً، فتظهر عدّادات المتأخر ناقصة.
- المراجعة: أنشئ مهمة، اجعل حالتها `IN_PROGRESS` وتاريخ استحقاقها في الماضي، ثم شغّل المسح — يجب أن تصبح `OVERDUE`.

**2) الصلاحيات تُقرأ من التوكن لا من قاعدة البيانات**
- الملف: `config/JwtAuthenticationFilter.java`.
- التغيير: تُبنى الصلاحيات من `userDetails.getAuthorities()` (المحمّلة من قاعدة البيانات/الكاش) بدل الدور المخزّن داخل التوكن.
- السبب: عند تغيير دور مستخدم، كان التوكن القديم يظل يحمل الدور القديم حتى انتهائه (24 ساعة).
- المراجعة: غيّر دور مستخدم من صفحة Team؛ يسري الدور الجديد في الطلب التالي (بعد إبطال الكاش أدناه) دون إعادة تسجيل دخول.

**3) إبطال كاش Redis عند تغيير كلمة المرور/الصورة/الدور**
- الملف: `service/UserService.java` (دالة `evictUserCache` + استدعاؤها في `changePassword` و`updateAvatar` و`updateUserRole`).
- السبب: كاش المستخدمين (`users`) يخزّن بصمة كلمة المرور والدور. بدون إبطاله كان بالإمكان **تسجيل الدخول بكلمة المرور القديمة بعد تغييرها**، وكانت تغييرات الدور لا تُطبَّق.
- المراجعة: غيّر كلمة المرور، ثم جرّب الدخول بالقديمة — يجب أن تُرفض فوراً.

**4) CORS قابل للضبط**
- الملفات: `config/SecurityConfig.java` + `application.properties` (`app.cors.allowed-origins`).
- السبب: كان المصدر (origin) مثبّتاً على `localhost:5173`، ما يمنع نشر الواجهة على أي دومين آخر.
- المراجعة: مرّر `CORS_ORIGINS` كمتغيّر بيئة (مضبوط في `docker-compose.yml`).

### ميزات الخادم

**5) تسجيل الدخول بالإيميل أو باسم المستخدم**
- الملفات: `dto/LoginRequest.java` (`identifier`)، `controller/AuthController.java` (`resolveEmailForLogin`)، `service/UserService.java`، `repository/UserRepository.java` (`findByUsername` / `findByEmailOrUsername`).
- السبب: ميزة مطلوبة. يُحلّ المُعرِّف إلى الإيميل القانوني قبل المصادقة، ويُمرَّر المُعرِّف غير المعروف كما هو ليفشل بأمان دون كشف وجود الحساب.
- المراجعة: سجّل الدخول مرة بالإيميل ومرة باسم المستخدم — كلاهما ينجح.

**6) حقل الاسم (name) بجانب اسم المستخدم**
- الملفات: `entity/User.java` (عمود `name`)، `dto/UserResponse.java`، `dto/UserSummaryResponse.java`، `dto/RegisterCompanyRequest.java` (`ownerName`)، `dto/UserRequest.java` (`name`)، وتعيينه في `UserService` عند التسجيل وإنشاء المستخدم.
- السبب: ميزة مطلوبة (اسم عرض ودود بجانب اسم المستخدم الفريد). العمود nullable حتى لا تتعطّل الصفوف القديمة.
- المراجعة: سجّل شركة بحقل الاسم، ثم `GET /api/users/me` يُعيد `name`.

### أخطاء الواجهة

**7) انتهاء الجلسة (401) يعيد التوجيه لتسجيل الدخول**
- الملفات: `context/AuthContext.jsx` (مستمع `auth:unauthorized` + تبسيط حارس `isMounted`)، والحدث يُطلَق من `api/client.js`.
- السبب: كان الحدث يُطلَق بلا مستمع، فيبقى المستخدم عالقاً بأخطاء 401 دون توجيه.
- المراجعة: احذف/أبطل التوكن ثم نفّذ أي طلب — يجب أن تُوجَّه إلى `/login`.

**8) قيمة الأولوية `CRITICAL` بدل `URGENT`**
- الملف: `pages/TaskBoard.jsx`.
- السبب: الفلتر كان يستخدم `URGENT` وهي **غير موجودة** في enum الخادم (`CRITICAL`)، فلا تُصفّى المهام الحرجة أبداً.
- المراجعة: صفِّ حسب الأولوية — يظهر خيار `CRITICAL` ويعمل.

**9) النوافذ المنبثقة لا تُغلق عند فشل الحفظ**
- الملفات: `pages/TaskBoard.jsx`، `pages/UsersPage.jsx`، `pages/CalendarPage.jsx`، `components/TaskDetailModal.jsx` (إعادة رمي الخطأ بعد عرض التنبيه).
- السبب: كانت الدوال الأب تبتلع الخطأ، فتُغلق النافذة وتفقد مدخلات المستخدم رغم فشل الطلب.
- المراجعة: تسبّب في فشل حفظ (مثلاً اسم مكرّر) — يجب أن تبقى النافذة مفتوحة مع رسالة الخطأ.

**10) تحديث اللوحات بعد المسح الدوري**
- الملفات: `pages/Dashboard.jsx`، `pages/KanbanBoard.jsx`، `pages/CalendarPage.jsx` (قراءة `sweepSignal` وإعادة التحميل).
- السبب: كانت هذه الصفحات تبقى ببيانات قديمة بعد كل مسح تلقائي.

**11) عرض الاسم في كل الواجهة**
- الملفات: أداة جديدة `utils/userDisplay.js` (`displayName`)، مستخدَمة في Topbar وProfile وUsers وقوائم المهام والرسائل والتعليقات.
- السبب: إظهار اسم العرض (ثم اسم المستخدم `@` ثم الإيميل).

**12) `.gitignore` وإزالة `node_modules` من التتبّع**
- السبب: كان `node_modules` مرفوعاً داخل المستودع (~10 آلاف ملف) ومعطوباً على منصّات أخرى؛ أُضيف `.gitignore` وأُزيل من تتبّع git.

---

## المرحلة 2 — مرفقات المهام (ملفات/صور)

- **الخادم**: `entity/Attachment.java`، `repository/AttachmentRepository.java`، `dto/AttachmentResponse.java`، `service/AttachmentService.java`، `controller/AttachmentController.java`، وتقديم الملفات ثابتاً عبر `config/WebConfig.java` تحت `/uploads/attachments/**`، وتنظيف الملفات عند حذف المهمة في `service/TaskService.java`.
- **الواجهة**: دوال `attachments` في `api/client.js`، ومكوّن `components/AttachmentSection.jsx` داخل نافذة تفاصيل المهمة.
- نقاط النهاية: `GET/POST /api/tasks/{id}/attachments` و`DELETE .../{attId}` (حتى 15MB؛ الحذف للرافع أو المُبلِّغ أو Admin).
- السبب: ميزة مطلوبة. التخزين محلي على القرص (نفس نمط الصور الرمزية).
- المراجعة: افتح مهمة، ارفع صورة (تظهر معاينة مصغّرة) وملفاً (أيقونة)، ثم افتح/نزّل واحذف. تأكّد أن حذف المهمة يحذف مرفقاتها من القرص.

---

## المرحلة 3 — رسائل لحظية + «سين» + حضور (أونلاين/أوفلاين)

- **الخادم**:
  - اعتماد جديد `spring-boot-starter-websocket` في `pom.xml`.
  - `config/WebSocketConfig.java`: نقطة STOMP على `/ws` مع مصادقة JWT في الـ handshake (اسم المستخدم في الجلسة = مُعرّف المستخدم).
  - `service/PresenceService.java`: تتبّع أونلاين/أوفلاين عبر أحداث الاتصال/الفصل مع عدّاد لكل مستخدم (يدعم عدّة تبويبات)، وبثّ التغييرات على `/topic/presence`.
  - `controller/PresenceController.java`: `GET /api/presence` (المستخدمون المتّصلون حالياً).
  - `service/MessageService.java`: دفع الرسالة فوراً للمستلم، وإرسال إشعار **قراءة (Seen)** للمُرسِل عند فتح المحادثة.
  - DTOs: `dto/PresenceEvent.java`، `dto/ReadReceipt.java`.
- **الواجهة**:
  - مكتبة `@stomp/stompjs`، ومزوّد `context/RealtimeContext.jsx` (اتصال واحد بالتوكن يشترك في الرسائل والقراءات والحضور).
  - ربطه في `pages/MessagesPage.jsx` و`components/ChatWindow.jsx`: وصول فوري للرسالة، نقطة أونلاين على الأفاتار، وكلمة **Seen** تحت آخر رسالة عند قراءتها.
- السبب: ميزة مطلوبة (وصول الرسائل لحظياً، السين، ومعرفة من متّصل).
- المراجعة: افتح المتصفح بحسابين (نافذة عادية + خفية). أرسل رسالة → تصل فوراً دون تحديث. لاحظ نقطة الأونلاين، وظهور «Seen» عند فتح الطرف الآخر للمحادثة.

> ملاحظة: التتبّع الحالي للحضور داخل الذاكرة (نسخة خادم واحدة). للتوسّع لأكثر من نسخة يلزم مخزن مشترك (Redis pub/sub).

---

## المرحلة 4 — فترة المسح 24 ساعة + كاش المهام

**فترة المسح 24 ساعة**
- الملفات: `scheduler/TaskScheduler.java` (المعدّل `fixedRate` = 24 ساعة)، وفي الواجهة `components/SchedulerPulse.jsx` (العدّاد يعرض «23h 59m») و`components/Sidebar.jsx` (النص «~24h»).
- السبب: طلب المستخدم تغيير الفترة من ~4 دقائق إلى 24 ساعة.

**كاش قوائم مهام المستخدم في Redis**
- الملف: `service/TaskService.java` — `@Cacheable(value="taskLists")` على الدوال الأربع (مهامي، المُسندة إليّ، مهام مساحة العمل، مهام الفريق) بمفاتيح لكل مستخدم، مع `@CacheEvict(allEntries=true)` على كل تعديل (إنشاء/تعديل/تغيير حالة/حذف). وأُضيف إبطال الكاش أيضاً في `TaskScheduler` لأن المسح يعدّل الحالات مباشرة في قاعدة البيانات.
- ولجعل الكاش يعمل مع تسلسل Redis، أصبح `dto/TaskResponse.java` و`dto/UserSummaryResponse.java` قابلَين للتسلسل (`Serializable`) — نفس نمط كاش المستخدمين.
- السبب: طلب المستخدم تسريع قراءة المهام الأكثر استخداماً. النهج المختار (إبطال كل قوائم المهام عند أي تعديل) يضمن عدم ظهور بيانات قديمة إطلاقاً.
- المراجعة: افتح «مهامي» مرتين متتاليتين — الأولى تضرب قاعدة البيانات والثانية تأتي من الكاش. أنشئ/عدّل مهمة ثم افتح القائمة — تظهر محدَّثة (لأن الكاش أُبطِل).

---

## تغييرات التشغيل (Ops)

- `docker-compose.yml`: أُضيفت خدمة **Postgres محلية** (فلا حاجة لبيانات قاعدة بيانات خارجية سرّية)، وحجم تخزين للمرفقات، ومتغيّر `CORS_ORIGINS` يشمل المنفذين 5173 و5174.
- `src/test/.../UserControllerTests.java`: صُحِّح ليطابق تواقيع `UserRequest`/`UserResponse` الجديدة (بعد إضافة حقل الاسم)، وإلا فشل بناء Docker الذي يصرّف الاختبارات.

---

## ملاحظات ومخاطر معروفة (اقتراحات لاحقة)

- استخدام **Flyway/Liquibase** بدل `ddl-auto=update` أأمن للإنتاج بعد إضافة أعمدة/جداول.
- **توكنات تجديد (refresh)** وإبطال عند تسجيل الخروج (التوكن حالياً صالح 24 ساعة).
- قصر بثّ الحضور على مستوى الـ **workspace** (حالياً `/topic/presence` عام).
- تنزيل المرفقات **بمصادقة** وفحص نوع/حجم أدقّ للملفات الحسّاسة.
- **Rate limiting** على الدخول/الرفع/الرسائل.
- تقسيم حزمة الواجهة (code-splitting)، وترقية `recharts` إلى v3، وإضافة اختبارات للميزات الجديدة.

---

*أُعِدّ هذا المستند آلياً بواسطة Claude (Cowork) لتوثيق التعديلات على فرع `عبدالرحمن`.*
